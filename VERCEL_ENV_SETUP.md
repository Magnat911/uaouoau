# 🚀 Почему "Failed to get token" и как исправить

## 📌 Суть проблемы

Ошибка `Failed to get token` означает, что переменные окружения **не установлены в Vercel**.

Файл `.env.example` — это просто **шаблон**, а не реальные переменные!

```
❌ .env.example        ← Шаблон (не читается Vercel)
✅ Vercel Dashboard    ← Реальные значения (работает)
✅ .env.local          ← Локальные значения (не в GitHub)
```

## ✅ Решение за 5 минут

### 1️⃣ Получи ключи из LiveKit

1. Открой https://livekit.cloud
2. Логируешься в аккаунт
3. Перейди в **Settings → API Keys**
4. Скопируй:
   - **API Key** → это `LIVEKIT_API_KEY`
   - **API Secret** → это `LIVEKIT_API_SECRET`
5. На той же странице найди **WebSocket URL** (вверху, обычно `wss://project-xxx.livekit.cloud`) → это `NEXT_PUBLIC_LIVEKIT_URL`

### 2️⃣ Добавь в Vercel Dashboard

1. Открой свой проект на https://vercel.com
2. Перейди в **Settings** (вкладка в верху)
3. Найди **Environment Variables** в левой панели
4. Добавь **три переменные** с точными именами:

```
LIVEKIT_API_KEY = [скопируй из livekit.cloud]
LIVEKIT_API_SECRET = [скопируй из livekit.cloud]
NEXT_PUBLIC_LIVEKIT_URL = [скопируй из livekit.cloud]
```

### 3️⃣ Переразверни проект

- Нажми **Redeploy** в Vercel (или сделай `git push`)
- Жди 2-3 минуты

Готово! Теперь нажми кнопку на сайте → должен подключиться к агенту.

## 🏠 Для локальной разработки

1. Создай файл `.env.local` в корне проекта:

```env
LIVEKIT_API_KEY=твой_ключ_из_livekit
LIVEKIT_API_SECRET=твой_секрет_из_livekit
NEXT_PUBLIC_LIVEKIT_URL=твой_wss_url_из_livekit
```

2. Запусти локально: `pnpm dev`
3. Нажми кнопку → работает!

**⚠️ Никогда не коммитй `.env.local` в GitHub!** (Уже в `.gitignore`)

## 🔍 Проверка что работает

### На локальной машине:
```bash
pnpm dev
# Открой http://localhost:3000
# Нажми кнопку → должен услышать микрофон
```

### На Vercel:
```bash
git push
# Жди деплоя
# Открой https://твой-проект.vercel.app
# Нажми кнопку → работает!
```

## ❓ Что если всё ещё не работает?

1. **Проверь что переменные добавлены правильно** → Settings → Environment Variables
2. **Нажми Redeploy** (иногда нужно пересобрать проект)
3. **Проверь консоль браузера** (F12 → Console) → там будет понятная ошибка
4. **Убедись что Sage-1242 агент запущен** на https://livekit.cloud/studio
